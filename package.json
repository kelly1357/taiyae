{
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "npx concurrently \"cd api && func start\" \"sleep 3 && cd client && npm run dev\" \"sleep 3 && open -a 'Google Chrome' http://localhost:5173\"",
    "install:all": "cd api && npm install && cd ../client && npm install"
  },
  "dependencies": {
    "react-router-dom": "^7.11.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.23",
    "concurrently": "^9.2.1",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18"
  }
}
